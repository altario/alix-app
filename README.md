# alix-dashboards

## Deploy

Run in Terminal:

- `brew install awscli` (you can install differently via [here](https://docs.aws.amazon.com/cli/latest/userguide/cli-chap-install.html))
- `nps deploy.s3`
