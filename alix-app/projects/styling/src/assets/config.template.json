{
  "base_url": "${URL}",
  "base_ws_url": "${WS_URL}",
  "appUrl": "${CLIENT_URL}",
  "cloudtasksId": "${CLOUDTASKS_ID}",
  "facebookAppId": "${FACEBOOK_APP_ID}",
  "stripe_key": "${STRIPE_KEY}",
  "taxamoToken": "${TAXAMO_TOKEN}",
  "apmServer": "${ELASTIC_APM_SERVER_URL}",
  "appVersion": "${APP_VERSION}"
}
